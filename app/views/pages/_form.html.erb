<%= form_for(page) do |f| %>
  <% if page.errors.any? %>
    <div id="error_explanation" class="alert alert-danger" role="alert">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<hr />
<!-- <div class="form-group">
	<%#= f.label "Select a category: (Hold command key ⌘ to select multiple)"%>
<%#= f.select :category_ids, Category.all.collect {|x| [x.name, x.id]}, {}, :multiple => true, class: "form-control" %>
  </div> -->
  

<div class="form-group">
    <%= f.label "Facebook Page URL:"%>
    <%= 
		f.text_field :origin_url, 
		class: "form-control", 
		placeholder: "https://www.facebook.com/example_page", 
		type: :url , 
		value: params[:url], 
		pattern: "^https:\/\/www.facebook.com\/(.*)$", 
		title: "Should be a valid facebook page url"
 %>

  </div>

<div class="form-group">
    <%= f.submit 'Add new page', class: "btn btn-success" %>
  </div>
<% end %>



<% if params[:url] %>
<script type="text/javascript">
$(function () {
	$("#new_page").submit()
})
</script> 
<% end %>